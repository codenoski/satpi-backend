services:
  - type: web
    name: satpi-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run GSBF_Cloud.py --server.port 10000 --server.address 0.0.0.0